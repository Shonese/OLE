# 要件定義書 v0.1

## 1. 目的

YouTube、X、Instagramなどを無意識に開く行動に短い摩擦を加え、だら見を抑える個人用ブラウザ拡張機能を作る。

厳格な遮断だけではなく、利用者がアクセスの意図を意識できる「介入」を中核体験とする。必要な利用は妨げすぎない。

## 2. 対象と公開方針

| 項目 | 内容 |
| --- | --- |
| 想定利用者 | 開発者本人 |
| 料金 | 無償 |
| MVP対応ブラウザ | Firefox |
| 将来対応ブラウザ | Chrome |
| 表示言語 | 日本語 |
| 初期対象サイト | YouTube、X、Instagram |

利用者は、任意のURLまたはドメインを対象サイトとして追加できる。

## 3. スケジュールとアクセス制御

### 3.1 スケジュール

- サイトごと・曜日ごとにルールを設定できる。
- 1曜日に複数の時間帯を登録できる。
- 開始・終了時刻は分単位で指定できる。
- スケジュール外の時間帯は自由にアクセスできる。

### 3.2 時間帯モード

| モード | 動作 |
| --- | --- |
| 自由 | 介入なしでアクセスを許可する。 |
| 介入 | 設定済みの介入を完了した後にアクセスを許可する。 |
| ブロック | 対象ページへのアクセスを遮断し、ブロック画面を表示する。 |

## 4. 介入

### 4.1 発動単位

サイトごとに、以下から選択できる。

- 対象サイトのページを開くたびに発動
- 同じタブ内のページ遷移でも発動
- 一度許可したら、そのタブを閉じるまで再介入しない

### 4.2 介入方式

以下を選択・組み合わせできる。

- 数秒の待機
- 呼吸アニメーション
- 閲覧目的の短文入力
- 注意文または今日の目標の表示
- 代替行動の提示

### 4.3 介入・ブロック画面

設定で以下の表示を切り替えられる。

- 最小限の文言と解除操作
- 呼吸やカウントダウンを中心にした画面
- 閲覧目的、代替行動、進捗を含む画面
- ライトテーマ／ダークテーマ

## 5. ブロック中の例外許可

ブロック時の例外許可方針を、サイトまたは時間帯ごとに設定できる。

- 一切許可しない
- 指定分数だけ許可する
- 理由入力後に許可する
- 複数回の確認操作後に許可する

時間限定許可の適用範囲は、以下から選択できる。

- 現在のタブだけ
- 同一サイトのすべてのタブ
- ブラウザ全体で同一サイト

ブロック中でも設定画面は開ける。ただし、設定変更前に確認操作を求める。

## 6. 設定画面

設定画面では以下を管理する。

- 対象サイトの追加・編集・削除
- サイトごとの介入発動単位
- 曜日・時間帯・モード
- 介入方式と画面表示
- 例外許可方針と時間限定許可の適用範囲
- テーマ
- 統計の消去

## 7. 統計

ローカルに以下を記録・表示する。

- サイト別のアクセス試行回数
- 曜日別・時間帯別のアクセス試行傾向

以下は記録しない。

- 閲覧した投稿、動画、検索語
- ページ本文
- ログイン情報
- 目的入力の内容
- 例外許可時の理由

## 8. データとプライバシー

- 設定と統計は端末上のブラウザ拡張機能ストレージにのみ保存する。
- アカウント、クラウド同期、分析基盤、広告、課金、外部API通信は使用しない。
- 利用者は設定画面から統計を消去できる。
- 拡張機能を削除した場合の保存データの扱いは、Firefoxの標準動作に従う。

## 9. 技術方針と制約

- Firefox向けにMVPを実装・検証する。
- 将来のChrome対応を前提に、標準的なWebExtensions APIを基本にする。
- Firefox固有APIへの依存を避け、ブラウザAPIの差異は互換レイヤーに集約する。
- UI、設定データ、スケジュール判定、統計ロジックはブラウザ非依存のモジュールとして分離する。
- 初期候補サイトに必要なアクセス権限だけを要求する。
- 任意サイトの追加時に、必要なアクセス権限を追加で要求する。
- 通信内容を監視・保存する方式は採用しない。
- ページ表示制御を主方式とする。
- 外部からコードを読み込まない。

## 10. MVP範囲

### 含める機能

- 初回設定
- 初期候補サイトと任意サイトの追加・編集
- 曜日別・分単位のスケジュール
- 自由／介入／ブロックの切替
- 待機、呼吸、目的入力、注意文・代替行動の介入
- 完全禁止、時間限定許可、理由入力、複数確認による例外許可
- サイト別、曜日別、時間帯別の基本統計
- ローカル保存と統計消去
- Firefox対応

### 含めない機能

- Chrome対応
- YouTube Shortsなど、ページ内要素単位の制御
- アカウントとクラウド同期
- 外部API連携
- 通知
- 推定節約時間
- 連続達成日数
- カレンダー形式の複雑な可視化

## 11. 受け入れ条件

1. YouTube、X、Instagramまたは任意ドメインを対象に登録できる。
2. 曜日ごとに複数の時間帯を分単位で登録し、各枠にモードを設定できる。
3. 対象サイトを開いたとき、該当時間帯のモードに応じて自由表示、介入、ブロックが行われる。
4. 介入モードでは、設定された介入を完了した後にのみ対象ページを閲覧できる。
5. ブロックモードでは、設定された例外許可方式に従ってのみ閲覧を許可できる。
6. アクセス試行回数をサイト別・曜日別・時間帯別に確認できる。
7. 設定・統計は端末内だけに保存され、外部へ送信されない。
8. MVPはFirefoxで動作する。
